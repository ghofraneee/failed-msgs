{
  "name": "failed-message-command-center",
  "version": "1.0.0",
  "private": true,
  "description": "Failed Message Command Center — batched external detection + dashboard snapshot",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "ingest": "node ingest.js",
    "kpi": "node main.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "axios": "^1.15.0"
  }
}
